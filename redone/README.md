Redone minimal PixelFlap app.

To run: place this folder under your webroot and visit:
http://localhost/codes/pixelforge/redone/

It uses your existing `pixelforge` MySQL database. Run `install.php` in the main project to create tables if needed.
